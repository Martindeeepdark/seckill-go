name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24'

    - name: Configure Go to bypass proxy for private modules
      run: go env -w GOPRIVATE=github.com/Martindeeepdark/*

    - name: Install protoc
      run: |
        sudo apt-get update
        sudo apt-get install -y protobuf-compiler

    - name: Install Go tools
      run: |
        go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
        go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest

    - name: Generate proto files
      run: make api

    - name: Download dependencies
      run: |
        cd api && go mod download && go mod tidy
        cd ../common && go mod download && go mod tidy
        for dir in services/*/; do
          (cd "$dir" && go mod download && go mod tidy)
        done

    - name: Build services
      run: make build

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: seckill-binaries
        path: build/
        retention-days: 7

    - name: Run tests
      run: make test

    - name: Run linter
      run: |
        go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
        make lint
